apiVersion: v1
kind: Service
metadata:
  name: event-service
  namespace: ticket-dev
spec:
  selector:
    app: event-service

  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082

  type: ClusterIP