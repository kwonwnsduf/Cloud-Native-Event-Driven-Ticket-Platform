apiVersion: v1
kind: Service
metadata:
  name: reservation-service
  namespace: ticket-dev
spec:
  selector:
    app: reservation-service

  ports:
    - protocol: TCP
      port: 8087
      targetPort: 8087

  type: ClusterIP