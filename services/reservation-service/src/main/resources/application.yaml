server:
  port: 8087

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ticket_reservation
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
