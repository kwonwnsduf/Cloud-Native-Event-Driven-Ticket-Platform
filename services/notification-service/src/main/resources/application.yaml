server:
  port: 8088

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ticket_notification
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true